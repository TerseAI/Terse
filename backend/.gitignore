node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma
/src/generated/local-auth-prisma

# Local-auth SQLite database (created by `prisma migrate dev` in local mode).
# Prisma resolves `file:./local-auth.db` relative to the schema file's directory.
/prisma/local-auth/local-auth.db
/prisma/local-auth/local-auth.db-journal

dist
node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma
/src/generated/local-prisma

# Local SQLite database (created by `prisma migrate dev` in self-host mode).
# Prisma resolves `file:./local.db` relative to the schema file's directory.
/prisma/local/local.db
/prisma/local/local.db-journal

dist
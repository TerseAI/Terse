[project]
name = "{{ PROJECT_NAME }}"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "{{ SDK_DEPENDENCY }}",
]

[tool.uv]
package = false
{% if USE_LOCAL_SDK_SOURCE %}

[tool.uv.sources]
terse-sdk = { path = {{ SDK_SOURCE_PATH }}, editable = true }
{% endif %}

from terse_sdk import JobDefinition
from terse_generated import Schedule, Terse


# After you connect integrations in Terse, rerun `terse generate` and
# import helpers like `GitHub`, `Slack`, or `Linear` from terse_generated.
job = JobDefinition(
    name="{{PROJECT_NAME}}",
    triggers=[Schedule.cron("0 9 * * 1")],
    skills=[Terse.skill()],
)


def main() -> None:
    print(job.model_dump())


if __name__ == "__main__":
    main()

# {{ PROJECT_NAME }}

Project scaffolded by `terse-cli`.

## Quick Start

```bash
uv sync
terse test
terse deploy
```

Edit `src/main.py` to register jobs with `@app.job(...)`.

## Generated Helpers

If you connect Attio or Snowflake in Terse, rerun `terse generate` to refresh `src/terse_generated.py`.

Import helpers like `Attio`, `AttioObject`, `Schedule`, `Snowflake`, and `TerseAgent` from `terse_generated`.

If a job registers `skills=[Snowflake.skill()]`, it can call deterministic wrappers like:

```python
agent.tools.snowflake.execute_query(query="select 1")
```

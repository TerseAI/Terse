# {{ PROJECT_NAME }}

Project scaffolded by `terse-python-cli`.

## Quick Start

```bash
uv sync
uv run python src/main.py
terse test
terse deploy
terse generate
```

Edit `src/main.py` to register jobs with `@app.job(...)`.

Connect Attio or Snowflake in Terse, then rerun `terse generate` to refresh `src/terse_generated.py`.

{{sectionHeader "Meta Ads resources"}}

{{> resource-class section.adAccountClass}}

{{#if toolFile}}
{{sectionHeader "Meta Ads tools"}}

{{> tools-slice toolFile}}

{{/if}}
{{sectionHeader "Meta Ads skill"}}

/** Meta Ads — read performance, create and pause ads, sync audiences, send conversions. Optionally scope to a default ad account and Page. */
export function metaAdsSkill(opts?: { adAccount?: MetaAdsAdAccount; pageId?: string }): TypedSkill<{{{section.skillToolType}}}> {
    return new MetaAdsOutputConfig("{{escape section.id}}", opts?.adAccount?.accountId ?? null, opts?.pageId ?? null)
}

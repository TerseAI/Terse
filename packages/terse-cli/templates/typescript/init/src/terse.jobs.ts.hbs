// This file is the job manifest. Every job lives in its own file under
// `src/jobs/`, and this file imports each one so Terse can register it.
// Add one import line per job — a job file that is never imported here
// silently never runs or deploys.
import "./jobs/sample-job"

// Auto-generated by `terse generate` — do not edit manually
// Re-run `terse generate` to update.

{{#if toc.length}}
// ═══ Contents ═══════════════════════════════════════════════
{{#each toc}}
// {{this}}
{{/each}}
// ════════════════════════════════════════════════════════════
{{/if}}

{{#if availableIntegrations}}
// The integrations currently available in Terse: {{availableIntegrations}}.
// Connect one with `terse integrate connect <type>`; list its tools with `terse integrate tool <type> --json`.
{{/if}}

{{#if useMultilineImports}}
import {
{{#each imports}}
    {{this}},
{{/each}}
} from "terse-sdk"
{{else}}
import { {{#each imports}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} } from "terse-sdk"
{{/if}}

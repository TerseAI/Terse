{{#if toolFiles.snowflake}}
{{sectionHeader "Snowflake tools"}}

{{> tools-slice toolFiles.snowflake}}

{{/if}}
{{sectionHeader "Snowflake skill"}}

/** Snowflake — run SQL queries against your warehouse */
export function snowflakeSkill(): TypedSkill<{{{snowflake.skillToolType}}}> {
    return new SnowflakeOutputConfig("{{escape snowflake.id}}")
}

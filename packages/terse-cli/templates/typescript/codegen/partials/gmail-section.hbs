{{sectionHeader "Gmail"}}

export const Gmail = {
    /** Trigger when a new email is received */
    onEmail(): TypedTrigger<GmailInputEvent> {
        return new GmailConfig("{{escape id}}", [GmailEventType.EMAIL_RECEIVED]) as TypedTrigger<GmailInputEvent>
    },
    /** Trigger on all Gmail events */
    trigger(opts?: { eventTypes?: GmailEventType[] }): TypedTrigger<GmailInputEvent> {
        return new GmailConfig("{{escape id}}", opts?.eventTypes) as TypedTrigger<GmailInputEvent>
    },
    /** Use in `skills[]` */
    skill(): TypedSkill<{{{skillToolType}}}> {
        return new GmailOutputConfig("{{escape id}}") as TypedSkill<{{{skillToolType}}}>
    },
    /** Use in `skills[]` — creates draft emails */
    draftSkill(): TypedSkill<{{{skillToolType}}}> {
        return new GmailDraftOutputConfig("{{escape id}}") as TypedSkill<{{{skillToolType}}}>
    },
}


---
title: "Quickstart"
description: "Create, test, and deploy your first Terse project"
icon: "rocket"
---

<Note>
    Terse is currently in private beta. [Email us at support@useterse.ai](mailto:support@useterse.ai?subject=Private%20beta%20access%20request&body=Hi%20Terse%20team%2C%0A%0AI%27d%20like%20to%20request%20access%20to%20the%20private%20beta.%20Here%20are%20the%20use%20cases%20I%27m%20looking%20to%20build%3A%0A%0A) to request access, and let us know what use cases you're looking to build.
</Note>

<Steps>
  <Step title="Install the CLI">

```bash
npm install -g terse-cli
```

  </Step>
  <Step title="Scaffold a project">

<CodeGroup>
```bash TypeScript
terse init my-project
```
</CodeGroup>

`terse init` handles the full setup in one command:

1. Scaffolds your TypeScript project files (`src/terse.jobs.ts`, config, etc.)
2. Installs dependencies
3. Opens the browser to authenticate via `terse login`
4. Checks your connected integrations and prompts you to add more if needed
5. Runs `terse generate` to produce a typed SDK from your workspace

After init, your project looks like this:

<CodeGroup>
```text TypeScript
my-project/
  src/
    terse.jobs.ts         # your workflow definitions (default entry file)
    terse.generated.ts    # generated SDK (do not edit)
  package.json
  tsconfig.json
  .env.example            # copy to .env when you need project-level env vars
  .gitignore
```
</CodeGroup>

`terse login` during init stores CLI credentials in your **user config**, not in the project folder. Copy **`.env.example`** to **`.env`** when you want `TERSE_API_KEY` (or other secrets) available to your app or tooling as environment variables.

  </Step>
  <Step title="Write your workflow">

Open `src/terse.jobs.ts` and define a workflow with `createJob()`. The scaffold includes a scheduled starter that runs a `TerseAgent` with structured output (install `zod` if you extend that pattern).

Before picking triggers or skills, read `src/terse.generated.ts`. It is the source of truth for what is connected in your workspace: `Triggers`, `Skills`, resource constants, and `toolbox` for deterministic tool calls. Re-run `terse generate` after you connect or disconnect integrations.

  </Step>
  <Step title="Test locally">

```bash
terse test
```

Runs your workflow locally against the Terse backend so you can verify it works before deploying.

  </Step>
  <Step title="Deploy">

```bash
terse deploy
```

Your code is packaged, uploaded, and hosted serverlessly. Navigate to the link provided to monitor your workflow. [Learn more about how deployment works.](/hosting)

  </Step>
</Steps>

## What to do next

<CardGroup cols={2}>
  <Card title="Context as Code" icon="lightbulb" href="/context-as-code">
    Understand what `terse generate` produces and why it matters.
  </Card>
  <Card title="Templates" icon="book-open" href="/templates">
    Start from a working workflow instead of a blank file.
  </Card>
</CardGroup>

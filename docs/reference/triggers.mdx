---
title: "Triggers"
description: "All available triggers that can start a Terse workflow."
icon: "zap"
---

Triggers define the event that starts your workflow. Each trigger is tied to an integration and fires when a specific event occurs. Your `onTrigger` handler receives a typed event object with the payload.

## Integration triggers

### GitHub

| Trigger | Event type | Description |
| ------- | ---------- | ----------- |
| `GitHub.onPROpened()` | `GithubPRInputEvent` | A pull request is opened |
| `GitHub.onPush()` | `GithubPushInputEvent` | Code is pushed to a branch |
| `GitHub.onIssueCreated()` | `GithubInputEvent` | An issue is created |

**Config:** Scoped to specific repositories via `repo` parameter.

<CodeGroup>
```ts TypeScript
triggers: [GitHub.onPROpened({ repo: Repos.MyOrg.MyRepo })]
```
```python Python
triggers=[
    TriggerConfig(
        integration_id=github_integration_id,
        integration_type="github",
        config_type="github",
        config={
            "repositoryIds": [repo_id],
            "eventTypes": ["pull_request.opened"],
        },
    )
]
```
</CodeGroup>

<Expandable title="Event payload">
  **All GitHub events include:**
  - `repository` - `{ id, name, owner, defaultBranch }`
  - `sender` - `{ login, email }`
  - `commits` - array of `{ sha, message, fileDiffs: [{ filename, diff }] }`

  **PR events add:**
  - `pullRequest` - `{ number, title, body, state, merged, head, base, author, url }`

  **Push events add:**
  - `branch` - the branch that was pushed to
</Expandable>

### Slack

| Trigger | Event type | Description |
| ------- | ---------- | ----------- |
| `Slack.onMessage()` | `SlackMessageEvent` | A message is posted in a channel |
| `Slack.onDm()` | `SlackMessageEvent` | A direct message is received |

**Config:** Scoped to specific channels or users.

<CodeGroup>
```ts TypeScript
triggers: [Slack.onMessage({ channel: SlackChannel.Engineering })]
```
```python Python
triggers=[
    TriggerConfig(
        integration_id=slack_integration_id,
        integration_type="slack",
        config_type="slack",
        config={"channelId": SlackChannel.Engineering.channel_id},
    )
]
```
</CodeGroup>

<Expandable title="Event payload">
  - `channelId`, `channelName` - where the message was posted
  - `userId`, `userName` - who sent it
  - `text` - message content
  - `timestamp`, `threadTs` - message timing and thread parent
  - `channelType` - `"channel"`, `"group"`, `"mpim"`, or `"im"`
  - `files` - array of attached files with `{ id, name, mimetype, url_private }`
  - `attachments` - rich message attachments
</Expandable>

### Linear

| Trigger | Event type | Description |
| ------- | ---------- | ----------- |
| `Linear.onIssueCreated()` | `LinearEvent` | An issue is created |
| `Linear.onIssueUpdated()` | `LinearEvent` | An issue is updated |

**Config:** Optionally scoped to a specific project.

<CodeGroup>
```ts TypeScript
triggers: [Linear.onIssueCreated({ project: LinearProject.Backend })]
```
```python Python
triggers=[
    TriggerConfig(
        integration_id=linear_integration_id,
        integration_type="linear",
        config_type="linear_input",
        config={"projectId": project_id},
    )
]
```
</CodeGroup>

<Expandable title="Event payload">
  - `data.action` - `"created"` or `"updated"`
  - `data.actor` - `{ name, email }`
  - `data.data` - issue details: `{ id, identifier, title, description, priorityLabel, state, team, assignee, labels, url }`
</Expandable>

### Attio

| Trigger | Event type | Description |
| ------- | ---------- | ----------- |
| `Attio.onRecordCreated()` | `AttioRecordInputEvent` | A record is created in a list |

**Config:** Scoped to a specific Attio list.

<CodeGroup>
```ts TypeScript
triggers: [Attio.onRecordCreated({ list: AttioList.Pipeline.NewDeals })]
```
```python Python
triggers=[
    TriggerConfig(
        integration_id=attio_integration_id,
        integration_type="attio",
        config_type="attio_output",
        config={"listId": list_id},
    )
]
```
</CodeGroup>

### Jira

| Trigger | Event type | Description |
| ------- | ---------- | ----------- |
| `Jira.onIssueCreated()` | `JiraEvent` | A Jira issue is created |
| `Jira.onIssueUpdated()` | `JiraEvent` | A Jira issue is updated |

**Config:** Optionally scoped to a specific project.

<CodeGroup>
```ts TypeScript
triggers: [Jira.onIssueCreated({ project: JiraProject.Backend })]
```
```python Python
triggers=[
    TriggerConfig(
        integration_id=jira_integration_id,
        integration_type="atlassian",
        config_type="jira",
        config={"projectKey": "BACKEND"},
    )
]
```
</CodeGroup>

<Expandable title="Event payload">
  - `data.user` - `{ displayName, emailAddress }`
  - `data.issue` - `{ key, fields: { summary, description, status, priority, project, issuetype, assignee, labels } }`
  - `data.comment` - present on comment events: `{ author, created, body }`
  - `data.changelog` - present on updates: `{ items: [{ field, fromString, toString }] }`
</Expandable>

### Gmail

| Trigger | Event type | Description |
| ------- | ---------- | ----------- |
| `Gmail.onNewEmail()` | `GmailEvent` | A new email is received |

<CodeGroup>
```ts TypeScript
triggers: [Gmail.onNewEmail()]
```
```python Python
triggers=[
    TriggerConfig(
        integration_id=gmail_integration_id,
        integration_type="gmail",
        config_type="gmail",
        config={},
    )
]
```
</CodeGroup>

<Expandable title="Event payload">
  - `data.subject` - email subject
  - `data.from`, `data.to` - sender and recipient
  - `data.body` - email body
  - `data.snippet` - short preview
  - `data.threadId`, `data.messageId` - for threading
  - `data.attachments` - array of `{ filename, mimeType, data }`
</Expandable>

### Figma

| Trigger | Event type | Description |
| ------- | ---------- | ----------- |
| `Figma.onFileComment()` | `FigmaCommentEvent` | A comment is added to a Figma file |

**Config:** Scoped to a specific file key.

<CodeGroup>
```ts TypeScript
triggers: [Figma.onFileComment({ fileKey: "abc123" })]
```
```python Python
triggers=[
    TriggerConfig(
        integration_id=figma_integration_id,
        integration_type="figma",
        config_type="figma",
        config={"fileKey": "abc123"},
    )
]
```
</CodeGroup>

<Expandable title="Event payload">
  - `data.fileKey` - the Figma file
  - `data.commentId`, `data.message` - the comment
  - `data.author` - `{ handle }`
  - `data.imageUrls` - `{ nodeImage, fullFrame }` screenshots of the commented area
  - `data.thread` - full comment thread with `{ id, author, message, resolvedAt }`
</Expandable>

### WorkOS

| Trigger | Event type | Description |
| ------- | ---------- | ----------- |
| `WorkOS.onEvent()` | `WorkOSInputEvent` | A WorkOS event occurs |

**Config:** Filtered by event types (`user.created`, `organization.created`, `organization_membership.created`, `invitation.created`, etc.).

<CodeGroup>
```ts TypeScript
triggers: [WorkOS.onEvent({ eventTypes: ["user.created"] })]
```
```python Python
triggers=[
    TriggerConfig(
        integration_id=workos_integration_id,
        integration_type="workos",
        config_type="workos_input",
        config={"eventTypes": ["user.created"]},
    )
]
```
</CodeGroup>

<Expandable title="Event payload">
  - `eventId`, `createdAt` - event metadata
  - `user` - present on user events: `{ id, email, firstName, lastName }`
  - `membership` - present on membership events: `{ id, userId, organizationId, role, status }`
  - `invitation` - present on invitation events: `{ id, email, organizationId, state }`
  - `organization` - present on organization events: `{ id, name }`
</Expandable>

## System triggers

### Schedule

| Trigger | Event type | Description |
| ------- | ---------- | ----------- |
| `Schedule.cron()` | `CronJobInputEvent` | Fires on a cron schedule |

<CodeGroup>
```ts TypeScript
triggers: [Schedule.cron("0 9 * * 1")] // Every Monday at 9am
```
```python Python
triggers=[Schedule.cron("0 9 * * 1")]  # Every Monday at 9am
```
</CodeGroup>

<Expandable title="Event payload">
  - `data.isManualTrigger` - whether the run was triggered manually from the dashboard
  - `data.manualContext` - optional context string provided on manual triggers
</Expandable>

### Webhook

| Trigger | Event type | Description |
| ------- | ---------- | ----------- |
| `Webhook.onRequest()` | `WebhookEvent` | Fires when an HTTP request hits the generated webhook URL |

Webhook URLs are auto-generated on deploy and remain stable across redeploys.

<CodeGroup>
```ts TypeScript
triggers: [Webhook.onRequest()]
```
```python Python
triggers=[
    TriggerConfig(
        integration_id="system",
        integration_type="webhook",
        config_type="webhook_input",
        config={},
    )
]
```
</CodeGroup>

<Expandable title="Event payload">
  - `body` - the raw JSON payload
  - `headers` - HTTP headers
  - `method` - HTTP method (GET, POST, etc.)
</Expandable>

## Common event interface

All events implement a shared interface:

<CodeGroup>
```ts TypeScript
interface InputEvent {
  readonly integrationType: string
  readonly eventType: string
  formatForAgentRunner(): string  // Formats the event for LLM consumption
}
```
```python Python
class InputEvent:
    integration_type: str
    event_type: str
    formatted_content: str  # Pre-formatted string for LLM consumption
```
</CodeGroup>

In TypeScript, call `event.formatForAgentRunner()` to get a string representation suitable for including in prompts. In Python, use `event.formatted_content`.
